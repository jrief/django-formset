from django.urls import path

{% for name, view in django_all_views.items() -%}
{{ view }}
{% endfor %}

urlpatterns = [
{%- for name, view in django_all_views.items() %}
    path('{{ name }}/', as_view()),
{% endfor -%}
]
